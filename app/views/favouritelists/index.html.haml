%h1 My Favourites

@tv_shows = tv_show.all
@favouritelists = favouritelist.all

%tbody
  - @favouritelists.each do |favouritelist|
    - if favouritelist.find(tv_show)
      %tr
        %td
          - if tv_show.image.attached?
            %div.polaroid
              = image_tag tv_show.image
              %div.container
                = link_to tv_show.name, tv_show
        %td
