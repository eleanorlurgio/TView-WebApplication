= form_for @favouritelist do |f|
  - if @favouritelist.errors.any?
    #error_explanation
      %h2= "#{pluralize(@favouritelist.errors.count, "error")} prohibited this favouritelist from being saved:"
      %ul
        - @favouritelist.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :user
    = f.text_field :user
  .actions
    = f.submit 'Save'
